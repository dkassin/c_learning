#include <stdio.h>

int main(void) {
    printf("Size of char: %lu\n", sizeof(char));
    printf("Size of int: %lu\n", sizeof(int));
    printf("Size of size_t: %lu\n", sizeof(size_t));
 
    return 0;
}